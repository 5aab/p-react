const logColor = (title, color) =>
    console.log(`New Color: ${title} | ${color}`);

const logColor2 = (title, color) =>
    console.log(`New Color: ${title} | ${color}`);

export {logColor};
export {logColor2};